# build output
dist/
.astro/
.wrangler/

# deps
node_modules/

# os / editor
.DS_Store
*.log

# python
__pycache__/
*.pyc
.venv/

# scratch
scratchpad/

# full-rollout data (see docs/full-rollout.md): the local parquet copy and the
# multi-GB per-jurisdiction build output. Never committed — the big files live in
# R2; only the small index.json/baselines.json/etc. stay in public/data.
locus-data/
data-build/

# remotion sizzle reel: rendered video + bundler cache (see docs/sizzle-reel.md).
# The remotion/ source IS committed; the rendered mp4 is a regenerable artifact.
out/
.remotion/
